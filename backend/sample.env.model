PORT=5000
MONGODB_URI=your_mongodb_connection_string
AIRTABLE_CLIENT_ID=your_airtable_oauth_client_id
AIRTABLE_CLIENT_SECRET=your_airtable_oauth_secret
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:5000
JWT_SECRET=your_jwt_secret
# For webhooks to work, BACKEND_URL must be HTTPS (Airtable requirement)
# For local development, use ngrok: ngrok http 5000
# Then set BACKEND_URL to the ngrok HTTPS URL (e.g., https://abc123.ngrok.io)
WEBHOOK_SECRET=your_airtable_webhook_secret
Airtable_BASEURL=https://airtable.com/oauth2/v1/authorize
Airtable_tokenUrl=https://airtable.com/oauth2/v1/token
WEBHOOK_SECRET=your_airtable_webhook_secret
